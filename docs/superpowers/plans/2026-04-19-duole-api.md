# Duole API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增一个独立脚本请求多乐剧集接口，并输出包含剧集ID、剧集名称、剧场名称、剧集标签、语言、推送时间的结构化结果。

**Architecture:** 采用一个单文件脚本承载请求和结构化转换逻辑，避免影响现有汇总脚本。测试使用独立 `unittest` 文件，优先验证结构化函数，再验证真实请求链路。

**Tech Stack:** Python 3.13、requests、unittest

---

### Task 1: 写失败测试锁定输出结构

**Files:**
- Create: `tests/test_duole_api.py`
- Test: `tests/test_duole_api.py`

- [ ] **Step 1: Write the failing test**

```python
import unittest

from duole_api import normalize_duole_items


class NormalizeDuoleItemsTests(unittest.TestCase):
    def test_normalize_keeps_requested_fields(self):
        raw_items = [{
            "story_id": "45238412",
            "story_name": "Die Geschiedene kehrt als Erbin zurück",
            "type": "MoboReels",
            "c_name": "隐藏身份,现代言情,复仇,反击,继承人",
            "language": 11,
            "publish_time": "2026-04-19 13:00:05",
        }]

        rows = normalize_duole_items(raw_items)

        self.assertEqual(rows, [{
            "剧集ID": "45238412",
            "剧集名称": "Die Geschiedene kehrt als Erbin zurück",
            "剧场名称": "MoboReels",
            "剧集标签": "隐藏身份,现代言情,复仇,反击,继承人",
            "语言": "德语",
            "推送时间": "2026-04-19 13:00:05",
        }])
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_duole_api -v`
Expected: FAIL with `ModuleNotFoundError` or missing `normalize_duole_items`

- [ ] **Step 3: Write minimal implementation**

```python
def normalize_duole_items(raw_items):
    ...
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m unittest tests.test_duole_api -v`
Expected: PASS

### Task 2: 实现独立脚本请求和输出

**Files:**
- Create: `duole_api.py`
- Modify: `tests/test_duole_api.py`
- Test: `tests/test_duole_api.py`

- [ ] **Step 1: Add fallback test for id/title/language**

```python
def test_normalize_uses_fallback_fields(self):
    raw_items = [{
        "_id": "44578415",
        "name": "بعد أن تركني، واعدت ابن عمه",
        "type": "MoboReels",
        "c_name": "现代言情,医生,总裁,复仇,角色成长",
        "lang_id": 10,
        "publish_time": "2026-04-19 13:00:04",
    }]

    rows = normalize_duole_items(raw_items)

    self.assertEqual(rows[0]["剧集ID"], "44578415")
    self.assertEqual(rows[0]["剧集名称"], "بعد أن تركني، واعدت ابن عمه")
    self.assertEqual(rows[0]["语言"], "阿拉伯语")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_duole_api -v`
Expected: FAIL because fallback behavior is not implemented yet

- [ ] **Step 3: Implement request helpers and main flow**

```python
def build_duole_headers():
    ...

def build_duole_payload():
    ...

def fetch_duole_response(...):
    ...

def main():
    ...
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m unittest tests.test_duole_api -v`
Expected: PASS

### Task 3: 真实请求验证

**Files:**
- Modify: `duole_api.py`

- [ ] **Step 1: Run script against live API**

Run: `python duole_api.py`
Expected: 输出 JSON 数组，包含 `剧集ID`、`剧集名称`、`剧场名称`、`剧集标签`、`语言`、`推送时间`

- [ ] **Step 2: Check response shape manually**

Expected:
- 真实请求成功
- 至少返回一条结构化数据
- 字段名与设计一致

### Self-Review

- 规格覆盖：请求、结构化、字段回退、语言映射、真实验证都已覆盖
- 无占位符：所有文件和命令已经明确
- 一致性：字段名统一为 `剧集ID`、`剧集名称`、`剧场名称`、`剧集标签`、`语言`、`推送时间`

## Execution Handoff

当前会话直接采用 inline 执行，不使用子代理。
